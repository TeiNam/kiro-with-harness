{
  "name": "kiro-with-harness",
  "version": "1.0.0",
  "description": "Kiro Harness — 프로필 기반 설치기와 steering·hook·agent·skill·MCP 자산 모음",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "validate:agents": "node scripts/validate-agents.js",
    "validate:models": "node scripts/validate-models.js"
  },
  "devDependencies": {
    "fast-check": "4.8.0"
  },
  "engines": {
    "node": ">=18"
  }
}
